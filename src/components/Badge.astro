---
type Variant = "available" | "out-of-stock" | "featured" | "new" | "neutral";

type Props = {
  variant: Variant;
  label: string;
};

const { variant, label } = Astro.props;
---

<span class:list={["badge", `badge--${variant}`]}>{label}</span>
